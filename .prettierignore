.next
node_modules
package-lock.json
docs/archive
next-env.d.ts
tsconfig.json

# Generated artifacts — owned by their generators, never reformatted by Prettier
# (reformatting would cause regeneration drift / unexplained diffs).
lib/db/schema/auth.generated.ts
drizzle/meta
drizzle/*.sql

# Vercel build-managed: `vercel build` rewrites vercel.json and creates .vercel/,
# so the in-build `verify` (format:check) must not check them.
vercel.json
.vercel
